классfigColor():
    def__init__(само,цвет):
        себя. цвет=цвет

прямоугольник класса(figColor):
    def__init__(self,width,height,color='red'):
        себя. ширина=ширина
        себя. высота=высота
        себя. цвет =фигЦвет(цвет))
    
    def getSquare(self):
        возврат(сам. ширина*сама. высота)
    
    def repr(self):
        возврат(сам. ширина, селфи. высота, селф. цвет. цвет, сам. getSquare())
        
классныйкруг():
    def__init__(self,radius):
        себя. радиус=радиус
        
    def getSquare(self):
        возврат(сам. радиус*математика. пи)
    
    def repr(self):
        возврат(сам. радиус, сам. цвет, сам. getSquare())

квадрат класса(прямоугольник):
    def__init__(self,side):
        себя. сторона =прямоугольник(сторона;сторона))
    
    def repr(self):
        возврат(сам. сторона. ширина, селфи. сторона. цвет. цвет, сам. сторона. getSquare())

a=прямоугольник(2,3;'красный')
b=квадрат(2)
печать(б. репр())
